<!DOCTYPE html>
  <html>
    <head>
      <title> The Latest Debate | Home</title>
      <%= csrf_meta_tag %>
      <%= javascript_include_tag :defaults %>

    </head>
    <body>
      <header>
       <% logo = image_tag("logo1.jpg", :alt => "The Latest Debate Logo", :class => "round") %>
       <%= link_to logo, root_path %>
      </header>
      <h1> What did you think of the latest elections? </h1>
      <div class="container">
           <section class="round">
             <% flash.each do |key, value| %>
               <div class="flash <%= key %>"><%= value %></div>
             <% end %>
             <table class="posts" summary="Posts">
               <%= render @posts %>
             </table>
             <%= will_paginate @posts %>
           </section>
      <%= debug(params) if Rails.env.development? %>
      </div>
    </body>
  </html>

